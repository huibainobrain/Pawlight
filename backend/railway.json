{
  "$schema": "https://railway.com/railway.schema.json",
  "build": {
    "builder": "NIXPACKS"
  },
  "deploy": {
    "startCommand": "npx prisma migrate resolve --applied 00000000000000_baseline && npx prisma migrate deploy && npm run start:prod",
    "healthcheckPath": "/health",
    "healthcheckTimeout": 30
  }
}
